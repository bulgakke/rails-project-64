nav.d-flex
  .m-2.me-auto Blog

  - if user_signed_in?
    .m-2 Signed in as #{current_user.username}
    .m-2 = button_to "Logout", destroy_user_session_path, method: :delete
  - else
    .m-2 = link_to "Sign up", new_user_registration_path
    .m-2 = link_to "Sign in", new_user_session_path
